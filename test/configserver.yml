info:
  description: Rosetta configuration files
  url: http://sa3act02.hotelbeds/repoconfig.git
---
spring:
  profiles: cloud
service:
  definition:
    metadata:
      uri: http://${spring.application.name}.${application.domain:cfapps.io}
      